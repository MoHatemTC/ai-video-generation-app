# Routes

Create API route files here.

Examples interns may create:

- `health.py`
- `videos.py`
- `scripts.py`
- `scenes.py`
- `jobs.py`

Routes should call services instead of containing large business logic. Long-running generation should be handled as an asynchronous job, not inside the request.
